# llm-scratch

松尾研LLM講座を中心にLLMの学習をスクラッチ実装するプロジェクト

---

## 数理的アプローチと本質的理解
本プロジェクトでは、単に既存のライブラリ（PyTorch等）を動かすだけでなく、その背景にある線形代数、確率論、最適化理論の数理構造を厳密に意識した実装を行います。

### 1. 勾配降下法（Gradient Descent）による最適化
目的関数（損失関数） $E(\mathbf{x})$ に対し、パラメータの更新式を以下のようにモデリングし、学習率 $\alpha$ の動的制御による収束性を検証します。
$$ \mathbf{x}_{t+1} = \mathbf{x}_t - \alpha \nabla E(\mathbf{x}_t) $$

### 2. 言語モデルの条件付き確率分布
LLMのテキスト生成は、与えられた単語の列（トークン列） $w_1, w_2, \dots, w_t$ に対する、次の単語 $w_{t+1}$ の条件付き確率分布の近似として捉えます。
$$ P(w_{t+1} \mid w_1, w_2, \dots, w_t) $$

---

## 今後の実装・学習予定
- [ ] PyTorchを用いた自動微分と基本最適化アルゴリズムの実装
- [ ] 『ゼロから作るDeep Learning ❷』をベースとしたAttention機構のPyTorch翻訳
- [ ] Transformerアーキテクチャのスクラッチ実装
